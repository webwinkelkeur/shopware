{% sw_extends '@Storefront/storefront/page/checkout/finish/index.html.twig' %}

{% block base_body_script %}
    {{ parent() }}
    {% if _ask_for_consent %}
        <script type="application/json" id="{{ _system_key }}_order_completed">
         {% autoescape 'js' %}
            {
           "webshopId": "{{ _shop_id }}",
          "orderNumber": "{{ page.order.orderNumber }}",
          "email": "{{ page.order.orderCustomer.email }}",
          "firstName": "{{ page.order.orderCustomer.firstName }}",
          "inviteDelay": "{{ _invite_delay }},"
            }
            {% endautoescape %}
        </script>
    {% endif %}
{% endblock %}
{#TODO add signature to the order data#}